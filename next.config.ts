import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // No backend yet — the UI runs entirely off the fixtures in data/demo.
  // See docs/STATUS.md for what is wired and what is still static.
};

export default nextConfig;
